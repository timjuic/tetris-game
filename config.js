const CONFIG = {
   // Board config
   BLOCKSIZE: 30,
   ROWS: 20,
   COLUMNS: 10,

   // Fall delay config
   STARTING_FALL_DELAY: 500,
   DELAY_DECREMENT_AMOUNT: 100,
   DECREMENT_EVERY_X_ROWS: 1,

   // Score config
   LINE_CLEAR_SCORE: 100,
   SCORE_MULTIPLIER: 2,
};

export default CONFIG